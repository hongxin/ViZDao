# 实操训练：大数据建模实验课 · 3 小时教案

> 授课日期：2026-06-24 下午 · 主讲：张宏鑫 · 平台：ViZDao（https://hongxin.github.io/ViZDao/）
> 主线：**「我建了一个模型，它好不好？—— 看一眼就知道。」**
> 受众：行业继续教育学员（税务/移动/电网等），有专业背景、编程能力一般、零安装即用。

## 教学机制（每单元 ≈ 13′）

```
讲(3′)：一句话动机 + 一张图     ← 知的种子
做(8′)：拖旋钮，实时重绘        ← 行（关键：即时反馈，不写代码）
悟(2′)：一句可带走的直觉 + AI 助教点评 ← 知的沉淀
```

每个动手单元学员只需打开 **https://hongxin.github.io/ViZDao/**，在左侧拖旋钮、右侧看图变化。**优雅降级**：标 ⚙️动手 的单元若届时未建成，按 🖥️讲授（用本课件/PPT 的图）顶上，主线不断。

---

## 时间轴（180′）

### 第 0 幕 · 为什么"看见"胜过"计算"（20′）
- **讲**：抛出主线问句。展示安斯库姆四重奏——四组数字均值/方差/相关系数几乎相同。
- **做** ⚙️[安斯库姆单元]：学员在 ViZDao 切换组 I→IV，**眼见**散点形状天差地别；组 IV 一个离群点撑起整条回归线。
  - 🖥️降级：用课件里的四重奏散点图讲解。
- **悟**：先画图，再算数。可视化是建模的眼睛。

### 第 1 幕 · 一维：给数据找规律（50′）
- **过拟合**（25′）⚙️[已上线·全场高潮之一]
  - **讲**：给带噪点拟合曲线，阶数越高越穿过每点——是学规律还是背答案？
  - **做**：学员拖「多项式阶数」到 12 → 曲线疯狂摆动、训练 MSE 极低但真值 MSE 暴涨；再拖「正则 λ」→ 一键驯服回平滑。**检查点**：调到明显过拟合再用 λ 救回（页面出 ✓）。
  - **悟**：过拟合 = 模型在背答案。正则 = 给模型"少记一点"的纪律。
- **分布 KDE**（25′）⚙️[拟建]
  - **讲**：同一组数，直方图分桶宽一变形状就变；密度图想摆脱这种武断。
  - **做**：拖「桶数」看直方图形状突变；拖「KDE 带宽」看密度曲线更稳。
  - 🖥️降级：用课件直方图/密度图对照讲解。
  - **悟**：直方图形状依赖分桶；密度图用带宽换更稳的形状感知。

### ☕ 休息（10′）

### 第 2 幕 · 多维：看见高维世界（55′）
- **高维困境 Iris**（12′）⚙️[拟建]
  - **讲**：每朵鸢尾花 4 个特征，纸面只有 2 维，怎么"看见"高维？
  - **做**：学员换 X/Y 轴特征对，体会"挑花萼两维分不清、挑花瓣两维泾渭分明"——挑哪两维本身就是建模。
  - **悟**：高维看不全 → 两条路：降维（压扁着看）、聚类（让数据自己分组）。
- **聚类 K-Means**（28′）⚙️[拟建·高潮之二]
  - **讲**：不告诉机器有几类，它能自己把相似的点聚到一起吗？
  - **做**：学员调「聚类数 K」，看簇怎么分；K 给错了会怎样；与真实三种花对比。
  - 🖥️降级：用课件 K-means 动图/手机日志聚类案例讲解。
  - **悟**：聚类没有唯一正确答案；方法与参数决定你"看见"几个群。
- **降维 PCA/t-SNE**（15′）🖥️[讲授为主，动手为冲刺]
  - **讲+演示**：MNIST 784 维 → t-SNE 自动聚成 10 个数字团；PCA 是糨糊、t-SNE 解团。用课件图/录屏演示。
  - **悟**：降维是有损的"压扁"；t-SNE 牺牲全局换局部，让聚团一眼可见。

### 第 3 幕 · 收束：可视化是 AI 的眼睛（25′）
- **可视化 × AI**⚙️[拟建·CTA]/🖥️
  - **做/演示**：把今天任意一张看不懂的图丢给 ViZDao 内置 **AI 助教**（DeepSeek），让它讲清楚每个团/每条曲线是什么。
  - **悟**：建模让数据有结构，可视化让结构被看见，AI 让看见可对话——这就是可视化建模的未来。
- **收尾**：回到主线一句话，留 3 个延伸钩子（插值族、平行坐标/散点矩阵、可视化×知识图谱）。

```
讲(≈35′) : 做(≈95′) : 悟+缓冲(≈50′)   实操占比 ≈ 53%
```

---

## 建造队列（今晚，性价比优先；标 ⚙️ 的单元）

| 优先级 | 单元 | 状态 |
|---|---|---|
| P0 | 多单元运行时 + 单元导航（前置） | 待建 |
| P1 | 安斯库姆开场（4 组散点切换，无建模） | 待建 |
| P2 | 高维困境 Iris（X/Y 维选择散点，按类着色） | 待建 |
| P3 | 分布 KDE（直方图 + 核密度，纯 TS） | 待建 |
| P4 | 聚类 K-Means（Iris + 纯 TS K-Means，簇着色） | 待建 |
| ✅ | 过拟合（已上线） | 完成 |
| 冲刺 | 降维 PCA（纯 TS）；t-SNE/UMAP 需库，明天多半讲授 | 视时间 |
| 收尾 | 收束×AI（CTA 页 + ask-AI） | 视时间 |

**现实目标 = P0–P4 + 过拟合 = 5 个动手单元。** 每建好一个即 push→Pages 自动部署，可在线即时验收。未建成的单元按 🖥️讲授降级，主线不受影响。

## 课前检查清单（明天上课前）
- [ ] https://hongxin.github.io/ViZDao/ 能打开、各单元可切换
- [ ] 过拟合检查点能出 ✓
- [ ] 内置 AI 助教填好 DeepSeek key（或现场演示填）
- [ ] 备好课件 PPT 作为降级与讲授素材
- [ ] 现场网络 / 学员设备能访问该 URL
